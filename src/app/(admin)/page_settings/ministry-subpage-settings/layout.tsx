import React, { Suspense } from "react";

const MinistryDepartmentSubPageLayout = ({ children }: { children: any }) => {
  return <Suspense>{children}</Suspense>;
};

export default MinistryDepartmentSubPageLayout;
